using UnityEngine;

[RequireComponent(typeof(PlayerMovement), typeof(PlayerJump), typeof(PlayerDash), typeof(PlayerAttack))]
public class PlayerController : MonoBehaviour
{
    private void Update()
    {
        // Player input and control flow
    }
}
